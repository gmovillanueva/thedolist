export enum Environments {
  PROD = 'production',
  DEV = 'development',
  TEST = 'test',
}

export enum EnvironmentFile {
  DEFAULT = '.env',
  PROD = '.env.prod',
  DEV = '.env.dev',
  TEST = '.env.test',
}
