export enum Envionments {
  PROD = 'production',
  DEV = 'development',
  TEST = 'test',
}

export enum EnvionmentFile {
  DEFAULT = '.env',
  PROD = '.env.prod',
  DEV = '.env.dev',
  TEST = '.env.test',
}
